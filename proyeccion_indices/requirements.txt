# Versiones con las que se validaron los resultados (los scripts instalan lo que falte automaticamente).
# Para reproducir exactamente las cifras: python -m pip install -r requirements.txt
openpyxl==3.1.5
numpy==2.4.6
pandas==3.0.6
scipy==1.17.1
statsmodels==0.15.0
matplotlib==3.11.2
